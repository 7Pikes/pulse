<div class="sixteen columns">
  <h1 class="remove-bottom" style="margin-top: 40px"> Pulse

    <div id="buttons" style="float: right">

      <a href="#" class="button" title="Демон отработал без ошибок?">
        Статус: <span style="color: green">Норм.</span></a>

      <a href="/default" class="button" style="margin-right: 6px">Показать все задачи</a>

    </div>

  </h1>

  <h5>
    Веб-мордочка для отображения текущих задач
  </h5>
  <hr />
</div>


<div class="one-third column">
  <h3>Пользователи</h3>
  <br/>

  <ul>
    <% @users.each do |user| %>

      <li>
        <a href="/blocked?id=<%= user.id %>">
          <%= user.name %></a>
      </li>

    <% end %>
  </ul>
</div>

<div class="two-thirds column">
  <% if @user %>
    <h3>Задачи для <%= @user.name %></h3>

    <p style="color: blue;">Показываются только заблокированные задачи</p>


    <% if @tasks.any? %>
      <ul class="square">

        <% @tasks.each do |task| %>
          <li>
            
            <strong><%= task.title %></strong> [<%= task.localized_phase %>]
            <br/>

            <%= task.description.gsub("\n", "<br/>") %>
            <br/>
            <br/>

            <a href="<%= task.global_in_context_url %>" target="_blank">Открыть карточку</a>
            <br/>
            <br/>

          </li>
        <% end %>

      </ul>
    <% else %>
      <p>Нет открытых задач.</p>
    <% end %>

  <% else %>
    <h3>Задачи</h3>
    <br/>

    <p>Пользователь не выбран</p>
  <% end %>
</div>
